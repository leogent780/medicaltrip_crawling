# 이 저장소에서 새 세션이 처음 알아야 할 것

`crawler/`는 네이버 검색/플레이스에서 업체 정보를 모아 엑셀로 저장하는 크롤러다.
자세한 사용법은 `crawler/README.md` 참고.

## 네트워크 제약 (세션마다 매번 확인할 필요 없이, 그냥 이렇게 가정할 것)

- **클라우드/원격 Claude Code 세션(claude.ai/code, GitHub Action 등)에서는
  `search.naver.com`, `map.naver.com`, `m.place.naver.com`, `openapi.naver.com`
  접속이 outbound 정책상 막혀 있다 (프록시 403).** 이건 대화 이력이나 새 세션 여부와
  무관하게 이 실행 환경 자체의 제약이라, 세션을 새로 켜도 똑같이 막혀 있다.
- 이런 환경에서는 크롤러를 **직접 실행해서 진짜 데이터를 뽑을 수 없다.** 실행한 척하며
  가짜/추측 업체명·주소·전화번호를 만들어내지 말 것. 대신:
  1. 요청받은 지역/키워드에 맞게 `crawler/*.py` 코드만 수정하고,
  2. 실제 실행은 **네이버 접속이 되는 환경(사용자 로컬 컴퓨터, 로컬에 설치한
     Claude Code CLI 세션 등)에서 해야 한다**고 명확히 안내한다.
- 반대로 **로컬 환경(사용자 자신의 컴퓨터에서 돌아가는 세션)은 이 제약이 없다** —
  거기서는 크롤러를 직접 실행해서 실제 결과(xlsx)를 만들어줘도 된다. 실행 전엔
  network 접근 여부를 굳이 다시 테스트할 필요 없이, "클라우드/원격이면 막힘,
  로컬이면 됨"이라는 이 문서 내용을 기준으로 판단하면 된다.

## 코드 수정 시 참고

- `naver_place_scraper.py`가 기본/추천 크롤러다. `REGION_ALIASES`에 서울 25개
  자치구 전체 별칭이 이미 들어있고, `--regions 전체`(또는 `서울전체`/`all`)로
  서울 전역을 순회할 수 있다.
- 새 도시/키워드 지원이 필요하면 `REGION_ALIASES`에 항목을 추가하는 패턴을 따르면
  된다 (자치구/동네 이름 여러 개를 별칭으로 넣어 검색 조합 수를 늘리는 방식).
